const fs = require('fs'), p = require('path');
const pub = (f) => fs.existsSync(p.join(__dirname, 'public', f));
const out = `// AUTO-GENERATED by scripts-logoflag.cjs — do not edit.
// Drop public/logo.png or public/music.mp3 in, re-run \`node scripts-logoflag.cjs\`, re-render.
export const HAS_LOGO_FILE = ${pub('logo.png')};
export const HAS_MUSIC_FILE = ${pub('music.mp3')};
`;
fs.writeFileSync(p.join(__dirname, 'src', 'logoFlag.ts'), out);
console.log(out.trim());
